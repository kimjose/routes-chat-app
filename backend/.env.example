# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# JWT Secret Key (generate a strong secret for production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Database Configuration (PostgreSQL)
DATABASE_URL=postgresql://username:password@localhost:5432/routes_chat_db

# API Keys for Mapping Services
# OpenRouteService API Key (free - get from https://openrouteservice.org/)
OPENROUTE_API_KEY=your-openroute-api-key

# Optional: Google Maps API Key (if you prefer Google Maps over OpenRouteService)
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Optional: Mapbox API Key (if you prefer Mapbox)
MAPBOX_API_KEY=your-mapbox-api-key

